## 1. Reason length handling

- [x] 1.1 Add shared truncation for classification reasons so valid labels are preserved when only the reason is too long
- [x] 1.2 Apply the same truncation rule to exception-derived fallback reasons

## 2. Verification

- [x] 2.1 Add or update classifier tests for overlong model reasons and overlong fallback reasons
- [x] 2.2 Run the relevant test and lint checks and update this task list to reflect completed work
