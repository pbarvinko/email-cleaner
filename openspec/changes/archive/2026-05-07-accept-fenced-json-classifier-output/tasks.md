## 1. Classifier response normalization

- [x] 1.1 Normalize classifier text responses by stripping one outer Markdown code fence when the entire response is fenced
- [x] 1.2 Keep existing invalid-response handling unchanged when normalized output is still not valid JSON

## 2. Verification

- [x] 2.1 Add or update classifier tests for raw JSON, ```json-fenced JSON, and bare fenced JSON responses
- [x] 2.2 Run the relevant test and lint checks and update this task list to reflect completed work
