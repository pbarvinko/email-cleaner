## 1. Local debug configuration

- [x] 1.1 Create a local `.vscode/launch.json` that runs `python -m email_cleaner` from the repository root using the shell environment

## 2. Ignore rule updates

- [x] 2.1 Update the root `.gitignore` to explicitly ignore `.opencode/` while keeping `.vscode/` and `.venv/` ignored

## 3. Verification

- [x] 3.1 Verify the launch configuration targets the supported module entrypoint and remains untracked by Git
- [x] 3.2 Update the change task list to reflect completed work
