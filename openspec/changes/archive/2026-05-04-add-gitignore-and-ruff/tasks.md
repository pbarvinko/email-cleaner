## 1. Repository hygiene

- [x] 1.1 Add a root `.gitignore` covering Python caches, virtual environments, pytest and Ruff caches, packaging artifacts, local environment files, and common OS/editor noise

## 2. Lint tooling

- [x] 2.1 Add Ruff to the project's dev dependencies in `pyproject.toml`
- [x] 2.2 Add minimal Ruff configuration in `pyproject.toml` using a small rule set suitable for the current codebase
- [x] 2.3 Fix any Ruff violations introduced by the selected rules so the repository lints cleanly

## 3. Developer documentation and verification

- [x] 3.1 Update `README.md` with a documented Ruff lint command
- [x] 3.2 Verify the documented install, test, and lint workflow locally and update the change task list to reflect completed work
